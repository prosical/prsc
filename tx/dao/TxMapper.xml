<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.myapp.tx.dao.ITXDAO">

	<insert id="insertStr10">
		insert into txone values (#{str}) 
	</insert>

	<insert id="insertStr30">
		insert into txtwo values (#{str}) 
	</insert>
</mapper>